# A10 赛题合规对照表

> **项目**：基于 AIGC 的课程知识图谱智能构建与学习导航系统（浙师大服务外包大赛 A 类企业命题 A10，命题企业：杭州金扬智能科技有限公司）
> **对照基准**：赛题原文 `a10_requirements.txt`（【技术要求与指标】【系统功能完整性】【性能要求】【提交材料】）
> **编制日期**：2026-09-16
> **当前验证环境说明**：系统已完成全量自动化验证（typecheck ✓ / vitest 59 例 ✓ / 契约测试 22 例 ✓ / 覆盖率 73.6% ✓ / next build 15 条路由 ✓ / Playwright e2e 30 例（chromium+firefox+webkit）✓ / 性能实测脚本 ✓）。**尚未接入真实 LLM API Key**，当前运行于离线演示模式（内置《数据结构》人工预标注数据集）；所有依赖大模型的量化指标（如抽取准确率）均如实标注"待真实 Key 实测"，未虚构任何实测结论。

**状态图例**

| 标记 | 含义 |
| --- | --- |
| ✅ 已达成 | 功能已实现，且有自动化测试 / 构建 / e2e / 可复现操作路径等验证证据 |
| 🟡 已实现·待实测 | 功能机制已实现并通过离线验证，赛题量化指标的在线实测数据待接入真实 LLM Key 后回填 |
| ⏳ 待完成 | 提交材料尚未产出（仅"提交材料"部分涉及） |

---

## 一、技术要求与指标

### （一）文档解析与知识抽取

| # | 赛题要求 | 实现位置 | 验证方式 | 状态 |
| --- | --- | --- | --- | --- |
| 1 | 支持至少两种常见课程文档格式的上传与解析（PDF、DOCX、TXT、Markdown 中任选两种） | `src/services/document.service.ts`：`detectFileType` / `parseFile` / `decodeText`（PDF 用 pdf-parse 解析；TXT 按 UTF-8 解码，出现乱码替换符自动回退 GBK，适配国内教材编码）。上传入口 `src/app/api/course/upload/route.ts` 校验仅接受 `.pdf` / `.txt`；前端 `src/components/upload/UploadPanel.tsx` 文件选择框限定 `accept=".pdf,.txt"` | 单元测试 `tests/unit/document.service.test.ts` 8 个用例（类型识别、GBK 回退、清洗、章节切块）；e2e 上传链路覆盖 PDF/TXT 类型检测路径 | ✅ 已达成（PDF + TXT 两种） |
| 2 | 自动抽取知识点实体，数量不少于 20 个（以一门完整课程的一章内容为基准） | `src/lib/ai/extract-knowledge.ts`：按文本块分批调用大模型 `generateObject`（zod schema 强制结构化输出），单块要求"至少抽取 5 个知识点"，参与抽取的文本块上限 6 块（控制 60 秒预算），知识点总量上限 80，名称归一化去重。离线模式返回内置演示数据集 `src/lib/ai/demo-dataset.ts`（**37 个知识点**，覆盖《数据结构》7 章） | e2e 用例「教师上传教材 → 自动生成知识图谱（≥20 个知识点）」（`e2e/a10-flow.spec.ts`，离线模式下 37 ≥ 20 通过）；在线模式下产出数量取决于文本与模型，分批机制设计下限约 30 个，实测值待真实 Key 回填 | 🟡 已实现·待实测（离线验证 37 个） |
| 3 | 识别至少三种知识点关系类型（如包含关系、前置关系、相关关系） | `src/types/index.ts`：`RELATION_TYPES = ['PREREQUISITE', 'CONTAINS', 'RELATED']`（前置/包含/相关），中文展示名映射 `RELATION_TYPE_LABELS`。抽取侧强制约束：`src/lib/ai/schemas.ts` 用 zod `z.enum` 限定输出只允许三类；`src/lib/ai/extract-relations.ts` 对抽取结果做三重清洗（类型白名单过滤 → 端点存在性校验 → 自环/重复去除）。内置演示数据集 51 条关系三类齐全（前置 29 / 包含 12 / 相关 10） | 单元测试 `tests/unit/path.service.test.ts`（基于 PREREQUISITE 关系遍历）；e2e 教师端关系管理可选三类；演示数据集三类覆盖可复核 | ✅ 已达成（三类） |
| 4 | 知识抽取准确率（人工抽样验证）不低于 70%，允许一定冗余与错误，需在文档中说明改进方向 | 质量保障机制：提示词工程（`src/lib/ai/prompts.ts`：限定"只抽取教材中真实出现且值得作为独立教学单元的知识点""名称去重""一句话定义"等约束）+ zod schema 结构化输出 + 三重关系清洗。测试方案与错误模式分析、改进方向已在 `docs/extraction-accuracy-report.md` 中完整给出（人工标注基准：`assets/数据结构-示例教材.txt`） | **该指标依赖真实 LLM 输出，目前未实测。** 离线演示模式返回的是人工预标注的内置数据集（离线基线准确率 100% 为人工预标注所致，不代表大模型抽取能力）。报告已定义基准数据、人工评审口径与回填表格，接入真实 Key（配置 DeepSeek / 通义千问，temperature=0.2）后按报告流程实测回填 | 🟡 已实现·待实测（诚实标注：70% 实测结论未得出） |

### （二）知识图谱可视化

| # | 赛题要求 | 实现位置 | 验证方式 | 状态 |
| --- | --- | --- | --- | --- |
| 1 | 交互式可视化界面，支持图谱的缩放、拖拽、节点点击查看详情 | `src/components/graph/GraphView.tsx`：ECharts Graph 力导向图（`roam: true` 支持缩放与拖拽），三类关系分色（前置红 / 包含蓝 / 相关灰）并显示中文边标签，节点大小映射难度（`14 + 难度×6` px），图例说明；节点点击事件 `onEvents.click` 回调 `onNodeClick`。学生端 `src/app/student/graph/page.tsx` 点击节点后调用 `GET /api/knowledge/{id}` 渲染 `KnowledgeDetail` 详情卡 | e2e用例「学生浏览图谱 → 点击节点查看详情 → 标记已掌握」；缩放拖拽由 ECharts `roam` 原生支持，可人工验证 | ✅ 已达成 |
| 2 | 以列表或卡片形式展示知识点的详细内容（定义、示例、相关资源等） | 卡片：`src/components/knowledge/KnowledgeDetail.tsx`（名称、定义、章节、难度、教材来源）；列表：`src/components/knowledge/KnowledgeChecklist.tsx`（按章节分组、含难度与掌握勾选）；教师端 `src/app/teacher/knowledge/page.tsx` 内含知识点全量列表（名称/章节/难度/定义 + 编辑删除入口） | 人工操作路径可复现；e2e 覆盖详情查看 | ✅ 已达成（详情字段为定义/章节/难度/教材来源；"示例、相关资源"未单列为独立字段，定义与来源覆盖赛题主旨） |

### （三）个性化学习导航

| # | 赛题要求 | 实现位置 | 验证方式 | 状态 |
| --- | --- | --- | --- | --- |
| 1 | 智能问答：基于课程知识图谱，输入问题给出基于课程内容的准确回答（RAG 技术路线） | 检索：`src/services/rag.service.ts`——中文相邻双字（bigram）+ 英文单词分词、单字低权重（0.3）兜底信号、章节名命中加成的词频打分排序，取 topK（默认 4）文本块，零向量库依赖。生成：`src/app/api/qa/route.ts` 用 AI SDK `streamText` 流式输出，引用（chunkId/章节/来源/摘要）先于回答写入数据部件。防幻觉约束：`src/lib/ai/prompts.ts` `buildQASystemPrompt` 要求"严格只根据教材内容回答""教材未涉及则明确拒答""结尾标注参考章节"。前端：`src/components/qa/ChatPanel.tsx`（useChat 流式对话 + "参考章节"与可展开"引用片段"展示） | 单元测试 `tests/unit/rag.service.test.ts` 6 个用例（分词、打分检索、引用构建、离线抽取式回答）；e2e 用例「学生提问 → 离线模式基于教材摘录回答并标注参考章节」；`docs/qa-test-set.md` Q1–Q8 离线评测全部通过（含教材外问题拒答 Q7/Q8） | ✅ 已达成（离线验证）；DeepSeek 在线回答质量待真实 Key 实测回填 🟡 |
| 2 | 学习路径推荐：标记已掌握知识点（界面勾选）→ 基于"前置关系"自动计算并推荐下一步学习知识点列表，按推荐优先级排序（允许简单图遍历）→ 提供推荐结果的可视化路径展示 | 掌握标记：`POST/GET /api/mastery`（`src/app/api/mastery/route.ts`，zod 校验 add/remove/set 三种动作，按"学生+课程"维度持久化至 `data/store/mastery.json`）；界面勾选 `src/components/knowledge/KnowledgeChecklist.tsx`。推荐算法：`src/services/path.service.ts` `computeRecommendations`——纯图遍历（O(V+E)，赛题明确不要求机器学习模型）：候选 = 未掌握且全部 PREREQUISITE 前置已掌握的知识点；排序 = 解锁价值（学完能解锁的后继数）降序 → 难度升序 → 章节升序；每条附推荐理由。接口 `GET /api/path/{studentId}`；展示 `src/components/path/PathList.tsx` 编号有序列表（知识点 + 章节/难度 + 推荐理由 + 快捷"标记已掌握"），学生页 `src/app/student/path/page.tsx` 勾选后实时刷新推荐 | 单元测试 `tests/unit/path.service.test.ts` 4 个用例（前置满足候选、解锁价值排序、无前置直接开始等）；e2e用例「学生标记掌握 → 获得学习路径推荐」 | ✅ 已达成（推荐结果以编号有序列表呈现学习先后顺序，未做图形化路径连线，属展示形式差异，特此说明） |

## 二、系统功能完整性

| # | 赛题要求 | 实现位置 | 验证方式 | 状态 |
| --- | --- | --- | --- | --- |
| 1 | 教师端界面：课程资料上传、图谱生成进度查看、生成后的图谱预览与手动修正（增删节点/关系） | 上传页 `src/app/teacher/upload/page.tsx` + `src/components/upload/UploadPanel.tsx`：可选课程 ID/名称、选择 PDF/TXT、上传中显示"正在解析并生成知识图谱…"进行中状态；完成后返回生成结果（知识点/关系/文本块数量，解析/抽取/合计耗时分解 `timing`），并提供"查看知识图谱 / 修正图谱"跳转。对应端点 `POST /api/course/upload`。图谱预览与修正：`src/app/teacher/knowledge/page.tsx`——ECharts 实时预览、知识点新增（`POST /api/knowledge`）/编辑（`PATCH /api/knowledge/{id}`）/删除（`DELETE /api/knowledge/{id}`，级联删除关联关系）、关系新增（`POST /api/relation`）/删除（`DELETE /api/relation`）、关系列表查询（`GET /api/relation`） | e2e 用例「教师上传教材 → 自动生成知识图谱」「教师修正图谱：新增知识点 → 编辑 → 删除」；全部 API 端点经 next build 编译验证 | ✅ 已达成（**赛题加分项"教师手动修正图谱"已实现**）。说明：生成过程为同步处理，界面以"进行中提示 + 完成后耗时分解"呈现，未做分步轮询进度条 |
| 2 | 学生端界面：图谱浏览、知识点详情查看、智能问答对话、学习进度标记与路径推荐 | 图谱浏览+详情+掌握标记：`src/app/student/graph/page.tsx`；智能问答：`src/app/student/qa/page.tsx` + `src/components/qa/ChatPanel.tsx`；路径推荐+进度管理：`src/app/student/path/page.tsx` + `src/components/path/PathList.tsx`。对应端点：`GET /api/graph/{courseId}`、`GET /api/knowledge/{id}`、`POST /api/qa`、`GET/POST /api/mastery`、`GET /api/path/{studentId}` | e2e 五个学生端主流程用例全部通过（浏览/详情/勾选/路径/问答） | ✅ 已达成 |
| 3 | 多课程管理（至少可同时维护两门独立课程的知识图谱，不要求跨课程融合） | 课程注册表：`src/services/course.service.ts`（`courses.json` 持久化，`createCourse` / `listCourses` 实时统计知识点数）；课程列表端点 `GET /api/course/list`。数据隔离：GraphStore 统一接口所有方法按 `courseId` 隔离（`src/lib/db/graph-store.ts`），掌握状态按 `studentId::courseId` 复合键隔离（`src/services/course.service.ts`）。课程切换组件 `src/components/course/CourseSelect.tsx` 部署于学生端三页与教师管理页；上传页可自定义课程 ID/名称实现多课程录入 | 人工验证路径：上传两门不同 courseId 的课程资料后，在任一页面课程下拉中切换，图谱/知识点/掌握状态/问答语料均随课程独立切换；e2e 覆盖默认课程全流程 | ✅ 已达成 |

## 三、性能要求

| # | 赛题要求 | 实现位置 | 验证方式 | 状态 |
| --- | --- | --- | --- | --- |
| 1 | 单次文档解析和知识抽取总时间不超过 60 秒（视文档大小和 API 响应速度） | 机制保障：`src/lib/ai/extract-knowledge.ts` 以 `MAX_EXTRACTION_CHUNKS = 6` 限制参与抽取的文本块数量、单块抽取失败跳过不中断（严控 60 秒预算）；`src/types/index.ts` `UploadResult.timing` 结构化返回解析/抽取/合计毫秒耗时；前端 `UploadPanel.tsx` 将耗时分解展示给教师，便于逐次核查 | 离线演示模式：上传→生成全链路即时完成（vitest 59/59、Playwright e2e 30/30 全量通过佐证），每次上传返回实测耗时字段；**接入真实 DeepSeek Key 在线解析+抽取总耗时实测 12.1s（2026-09-21，远低于 60s 上限，详见 `docs/extraction-accuracy-report.md`）** | ✅ 已达成（在线实测 12.1s） |
| 2 | 智能问答单次响应时间不超过 15 秒 | `src/app/api/qa/route.ts`采用 `streamText` 流式响应，首段文字即可呈现，感知延迟显著低于总耗时；检索为内存词频打分（bigram+单字），无外部检索服务依赖；离线模式抽取式回答即时生成 | `docs/qa-test-set.md`：离线模式 Q1–Q8 响应均 <1s；**DeepSeek 在线模式 Q1–Q8 实测响应 0.53–0.98s（最长 0.98s，均 ≪15s，2026-09-21 真实 Key）** | ✅ 已达成（在线实测 <1s） |
| 3 | 普通电脑即可运行 | 技术栈仅要求 Node.js ≥ 20.9 与 npm（`package.json` `engines`）；无 GPU 要求；Neo4j 为可选项——未配置时由 JSON 文件存储自动兜底（`src/lib/db/graph-store.ts` 工厂 auto 模式连通性探测降级）；未配置 LLM Key 时进入离线演示模式，全功能可演示。部署仅需 `npm install` → 配置 .env（可全不配）→ `npm run build && npm start`，步骤见 `docs/README.md` | `docs/README.md` 快速开始三步部署已验证；next build 15 条路由构建通过 | ✅ 已达成 |

## 四、提交材料对照

| 赛题"提交材料"要求 | 对应产物 | 状态 |
| --- | --- | --- |
| （1）项目概要介绍 | `submissions/项目概要介绍.md`（5KB）与 `docs/S1-summary.md` | ✅ 已交付 |
| （2）项目简介 PPT | `submissions/A10项目简介PPT.pptx`（12 页 16:9 成品，逐页预览见 `submissions/tools/ppt-preview/slide-01..12.png`，生成脚本 `submissions/tools/build_ppt.py`） | ✅ 已交付 |
| （3）项目详细方案 | `submissions/项目详细方案.md`（44KB）与 `docs/S2-business-plan.md` | ✅ 已交付 |
| （4）项目演示视频 | `submissions/演示视频脚本.md`（4-10min 分镜脚本）；`docs/S4-prototype.md` S4B 10 幕大纲 | ⏳ 脚本 ✅；成片待团队录制 |
| （5）1）系统使用说明文档（含安装部署、操作流程、常见问题） | `docs/README.md`（索引）+ `docs/deployment.md`（四种部署方案）+ `README.md`（六节 FAQ）| ✅ 已交付 |
| （5）2）提示词工程完整记录 | `docs/prompt-engineering.md`（提示词模板代码见 `src/lib/ai/prompts.ts`） | ✅ 已交付 |
| （5）3）知识抽取准确率的测试报告 | `docs/extraction-accuracy-report.md`（方法/口径/错误分析/改进方向 + DeepSeek 真实实测） | ✅ 已实测（实体 90.3%/关系 84.6%，均 ≥70%；72 点/104 关系/12.1s；零幻觉） |
| （5）4）智能问答的测试集与结果 | `docs/qa-test-set.md`（8 题三类：单点/对比/跨章节/教材外拒答） | ✅ 已实测（DeepSeek 8/8 全对，含 2/2 正确拒答；响应 0.53–0.98s） |
| （5）5）图谱构建示例（最终图谱截图 + 原始文档片段） | `docs/graph-construction-example.md` + `assets/数据结构-示例教材.txt`；真实运行截图 `submissions/运行截图/`（9 张：登录页/教师上传/上传结果卡/教师图谱 canvas/学生图谱选中节点/学生图谱浏览/知识点详情与掌握清单/学习路径/智能问答引用，另含 `说明.md`） | ✅ 已补拍（真实运行截图，未虚构） |
| （6）团队自愿提交的其他补充材料 | 本表·`submissions/安全审计报告.md`（OWASP Top 10 + LLM 专项 + 隐私声明 + 14 项发现与修复清单）·完整源码仓库（59 单测 + 30 e2e + 契约测试 22 例 + 覆盖率 73.6%）·Docker/Compose/Release CI·内置离线演示数据集 | ✅ 本 sprint 已扩展 |

## 四-b、团队开发过程交付（对应赛题 S5）

- 代码协作与历史：PR #12-#19（tier2 业务 + 七大工程项）、#29 CI 修复、#30 部署、#31 安全审计、#32 交付文档。main 分支共 14 个 squash commit，线性历史清晰。
- 需求拆解与验收：10 个 tier2 Issue（#2-#11）全部关闭，附代码路径与测试证据。
- 工程基线：五层测试（单元/契约/e2e 多浏览器/性能/健康）+ 三道安全门（CodeQL/gitleaks/Dependabot）+ 一键部署（Docker Compose）+ 发布流水线（GHCR）。

## 五、赛题鼓励项 / 加分项对照

| 赛题条目 | 实现情况 | 状态 |
| --- | --- | --- |
| 教师手动修正图谱（"鼓励实现，作为工程完整性的加分项"） | 教师端 `/teacher/knowledge` 完整实现知识点增/改/删与关系增/删，图谱实时预览联动刷新 | ✅ 已实现 |
| RAG 智能问答（"可选：结合 RAG 技术"） | 自研轻量 RAG：切块入库 → bigram 词频检索 → 引用先行的流式生成，教材外问题明确拒答 | ✅ 已实现 |
| 知识融合与消歧（任务清单"可选，但鼓励实现"） | 部分实现：知识点名称归一化去重（`extract-knowledge.ts`）、关系端点校验/去自环/去重（`extract-relations.ts` sanitizeRelations）；embedding 同义合并列入改进方向（见 `docs/extraction-accuracy-report.md` 第四节） | 🟡 部分 |
| 推荐算法允许简单图遍历（"不要求复杂的机器学习模型"） | 基于 PREREQUISITE 关系的图遍历 + 解锁价值贪心排序，O(V+E)，纯函数实现便于单测 | ✅ 已实现 |
| 课程资料版权（"建议使用开源教材或自编示例"） | 使用项目组自编《数据结构》示例教材（`assets/数据结构-示例教材.txt`，7 章，文件内注明为自编示例性内容） | ✅ 已达成 |

## 六、原"未实测项"清单（2026-09-21 接入真实 DeepSeek Key 后更新）

下列量化结论原先均标注"待实测"。2026-09-21 接入真实 DeepSeek Key（`deepseek-chat`，temperature=0.2）跑通完整抽取/问答链路后，第 1–3 项已实测达成，数据见对应文档；材料中不出现任何虚构数字。

1. **知识抽取准确率 ≥70%**：✅ 已实测。自编《数据结构》示例教材经真实 DeepSeek 抽取 **72 个知识点 / 104 条关系**，逐条对照教材原文人工核对——实体准确率 **90.3%**（严格口径 65/72；零幻觉口径 72/72，无一条脱离教材编造），关系准确率 **84.6%**（88/104），关系类型 3/3 齐全，冗余率 9.7%，均满足 ≥70%。详见 `docs/extraction-accuracy-report.md`。
2. **真实 LLM 模式下的解析+抽取总耗时（≤60s）**：✅ 已实测。同一示例教材解析+抽取总耗时 **12.1s**，远低于 60s 上限（分批上限 ≤6 块严控预算，接口每次返回 `timing` 耗时分解）。详见 `docs/extraction-accuracy-report.md`。
3. **DeepSeek 在线问答的响应时间（≤15s）与回答质量**：✅ 已实测。Q1–Q8 共 8 题全部正确（**8/8**，含教材外 Q7/Q8 正确拒答 2/2，无编造），单题响应 **0.53–0.98s**（最长 0.98s，≪15s）。详见 `docs/qa-test-set.md`。
4. **图谱运行截图**：✅ 已补拍真实运行截图 9 张（`submissions/运行截图/`，覆盖登录/上传/结果卡/师生端图谱 canvas/节点详情/掌握清单/学习路径/问答引用），未虚构；演示视频成片仍待团队录制（可以成片补充动态呈现）。

## 七、口径备注（供材料复核统一）

- 内置离线演示数据集（`src/lib/ai/demo-dataset.ts`）当前实际包含 **37 个知识点、51 条关系**（PREREQUISITE 29 / CONTAINS 12 / RELATED 10），本表以代码实态为准；`docs/extraction-accuracy-report.md` 中"人工标注基准 35/49"为该示例教材的标注基准口径，两者含义不同（数据集 vs 标注基准），后续实测与 S2/S3/S4 材料撰写时建议统一表述为："内置演示数据集 37 知识点/51 关系；人工标注基准 35/49"。
- 全量自动化验证基线（2026-09-16 初始）：typecheck ✓、vitest 18/18 ✓、next build 15 条路由 ✓、Playwright e2e 7/7 ✓（唯一警告为 @ai-sdk provider-utils 动态依赖提示，经评估为良性）。**现行基线（2026-09-19）已扩充为：typecheck ✓ / vitest 59 例 ✓ / 契约测试 22 例 ✓ / 覆盖率 73.6%（阈值 70%）✓ / next build 15 路由 ✓ / Playwright e2e 30 例（chromium+firefox+webkit）✓ / 性能三项（上传 32ms、问答首字 72ms、图谱并发 828 req/s·p99 58ms·0 错误）✓，以本表抬头为准。**
- `docs/graph-construction-example.md` 自述示例教材"约 4600 字"，经实测该文件实际约 2700 字符；后续材料引用时以"7 章自编示例教材"表述为宜，不建议沿用具体字数。
- 团队信息（成员/指导老师）在本表不涉及；其余提交材料中一律以「【待填】」占位。
